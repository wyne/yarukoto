diff --git a/node_modules/react-native-sortables/.DS_Store b/node_modules/react-native-sortables/.DS_Store
new file mode 100644
index 0000000..2dbc806
Binary files /dev/null and b/node_modules/react-native-sortables/.DS_Store differ
diff --git a/node_modules/react-native-sortables/dist/typescript/constants/props.d.ts b/node_modules/react-native-sortables/dist/typescript/constants/props.d.ts
index 1553aa5..827d2b1 100644
--- a/node_modules/react-native-sortables/dist/typescript/constants/props.d.ts
+++ b/node_modules/react-native-sortables/dist/typescript/constants/props.d.ts
@@ -24,6 +24,7 @@ export declare const DEFAULT_SHARED_PROPS: {
     dimensionsAnimationType: "none";
     dragActivationDelay: number;
     dragActivationFailOffset: number;
+    dragActivationDistance: number;
     dropAnimationDuration: number;
     DropIndicatorComponent: typeof DefaultDropIndicator;
     dropIndicatorStyle: ViewStyle;
diff --git a/node_modules/react-native-sortables/dist/typescript/types/props/shared.d.ts b/node_modules/react-native-sortables/dist/typescript/types/props/shared.d.ts
index 99b326a..e499d79 100644
--- a/node_modules/react-native-sortables/dist/typescript/types/props/shared.d.ts
+++ b/node_modules/react-native-sortables/dist/typescript/types/props/shared.d.ts
@@ -29,6 +29,7 @@ export type ItemDragSettings = AnimatableProps<{
     activationAnimationDuration: number;
     dropAnimationDuration: number;
     dragActivationFailOffset: number;
+    dragActivationDistance: number;
 }> & {
     overDrag: OverDrag;
     reorderTriggerOrigin: ReorderTriggerOrigin;
diff --git a/node_modules/react-native-sortables/src/constants/props.ts b/node_modules/react-native-sortables/src/constants/props.ts
index f779d26..2ad1958 100644
--- a/node_modules/react-native-sortables/src/constants/props.ts
+++ b/node_modules/react-native-sortables/src/constants/props.ts
@@ -30,6 +30,7 @@ export const DEFAULT_SHARED_PROPS = {
   dimensionsAnimationType: 'none',
   dragActivationDelay: 200,
   dragActivationFailOffset: 5,
+  dragActivationDistance: Infinity,
   dropAnimationDuration: 300,
   DropIndicatorComponent: DefaultDropIndicator,
   dropIndicatorStyle: {
diff --git a/node_modules/react-native-sortables/src/providers/shared/CommonValuesProvider.ts b/node_modules/react-native-sortables/src/providers/shared/CommonValuesProvider.ts
index 52800b8..5d8f070 100644
--- a/node_modules/react-native-sortables/src/providers/shared/CommonValuesProvider.ts
+++ b/node_modules/react-native-sortables/src/providers/shared/CommonValuesProvider.ts
@@ -61,6 +61,7 @@ const { CommonValuesContext, CommonValuesProvider, useCommonValuesContext } =
     controlledItemDimensions,
     customHandle,
     dragActivationDelay: _dragActivationDelay,
+    dragActivationDistance: _dragActivationDistance,
     dragActivationFailOffset: _dragActivationFailOffset,
     dropAnimationDuration: _dropAnimationDuration,
     enableActiveItemSnap: _enableActiveItemSnap,
@@ -117,6 +118,7 @@ const { CommonValuesContext, CommonValuesProvider, useCommonValuesContext } =
     const dragActivationFailOffset = useAnimatableValue(
       _dragActivationFailOffset
     );
+    const dragActivationDistance = useAnimatableValue(_dragActivationDistance);
     const dropAnimationDuration = useAnimatableValue(_dropAnimationDuration);
 
     // ACTIVE ITEM DECORATION
@@ -193,6 +195,7 @@ const { CommonValuesContext, CommonValuesProvider, useCommonValuesContext } =
         controlledItemDimensions,
         customHandle,
         dragActivationDelay,
+        dragActivationDistance,
         dragActivationFailOffset,
         dropAnimationDuration,
         enableActiveItemSnap,
diff --git a/node_modules/react-native-sortables/src/providers/shared/DragProvider.ts b/node_modules/react-native-sortables/src/providers/shared/DragProvider.ts
index 3dbaf7d..8dc7449 100644
--- a/node_modules/react-native-sortables/src/providers/shared/DragProvider.ts
+++ b/node_modules/react-native-sortables/src/providers/shared/DragProvider.ts
@@ -89,6 +89,7 @@ const { DragProvider, useDragContext } = createProvider('Drag')<
     containerId,
     containerWidth,
     dragActivationDelay,
+    dragActivationDistance,
     dragActivationFailOffset,
     dropAnimationDuration,
     enableActiveItemSnap,
@@ -377,6 +378,52 @@ const { DragProvider, useDragContext } = createProvider('Drag')<
     ]
   );
 
+  /**
+   * Lifts the touched item. Reached either by the activation delay elapsing or,
+   * when `dragActivationDistance` is set, by the touch travelling that far —
+   * whichever happens first.
+   */
+  const activateTouchedItem = useCallback(
+    (
+      touch: TouchData,
+      key: string,
+      activationAnimationProgress: SharedValue<number>,
+      activate: () => void
+    ) => {
+      'worklet';
+      if (!usesAbsoluteLayout.value) {
+        return;
+      }
+
+      const itemPosition = itemLayoutPositions.value[key];
+      const itemDimensions = getItemDimensions(
+        key,
+        itemWidths.value,
+        itemHeights.value
+      );
+
+      if (!itemPosition || !itemDimensions) {
+        return;
+      }
+
+      handleDragStart(
+        touch,
+        key,
+        itemPosition,
+        itemDimensions,
+        activationAnimationProgress
+      );
+      activate();
+    },
+    [
+      handleDragStart,
+      itemHeights,
+      itemWidths,
+      itemLayoutPositions,
+      usesAbsoluteLayout
+    ]
+  );
+
   const handleTouchStart = useCallback(
     (
       e: GestureTouchEvent,
@@ -411,29 +458,7 @@ const { DragProvider, useDragContext } = createProvider('Drag')<
       // Start handling touch after a delay to prevent accidental activation
       // e.g. while scrolling the ScrollView
       ctx.activationTimeoutId = setAnimatedTimeout(() => {
-        if (!usesAbsoluteLayout.value) {
-          return;
-        }
-
-        const itemPosition = itemLayoutPositions.value[key];
-        const itemDimensions = getItemDimensions(
-          key,
-          itemWidths.value,
-          itemHeights.value
-        );
-
-        if (!itemPosition || !itemDimensions) {
-          return;
-        }
-
-        handleDragStart(
-          touch,
-          key,
-          itemPosition,
-          itemDimensions,
-          activationAnimationProgress
-        );
-        activate();
+        activateTouchedItem(touch, key, activationAnimationProgress, activate);
       }, dragActivationDelay.value);
     },
     [
@@ -452,7 +477,13 @@ const { DragProvider, useDragContext } = createProvider('Drag')<
   );
 
   const handleTouchesMove = useCallback(
-    (e: GestureTouchEvent, fail: () => void) => {
+    (
+      e: GestureTouchEvent,
+      fail: () => void,
+      key?: string,
+      activationAnimationProgress?: SharedValue<number>,
+      activate?: () => void
+    ) => {
       'worklet';
       const touch = e.allTouches[0];
       currentTouch.value = touch ?? null;
@@ -483,6 +514,22 @@ const { DragProvider, useDragContext } = createProvider('Drag')<
         // Cancel touch if the touch moved too far from the initial position
         // before the item activation animation starts
         const r = Math.sqrt(dX * dX + dY * dY);
+
+        // Travelling far enough is itself an intent to drag, so activate now
+        // rather than waiting out the delay. Checked before the fail offset,
+        // which would otherwise cancel the very movement that means to drag.
+        if (
+          activeItemKey.value === null &&
+          r >= dragActivationDistance.value &&
+          key !== undefined &&
+          activationAnimationProgress !== undefined &&
+          activate !== undefined
+        ) {
+          clearAnimatedTimeout(ctx.activationTimeoutId);
+          activateTouchedItem(touch, key, activationAnimationProgress, activate);
+          return;
+        }
+
         if (
           // activeItemKey is set after the drag activation delay passes
           // and we don't want to cancel the touch anymore after this time
@@ -495,8 +542,10 @@ const { DragProvider, useDragContext } = createProvider('Drag')<
       }
     },
     [
+      activateTouchedItem,
       activationState,
       activeItemKey,
+      dragActivationDistance,
       dragActivationFailOffset,
       currentTouch,
       context,
diff --git a/node_modules/react-native-sortables/src/providers/shared/hooks/useItemPanGesture.ts b/node_modules/react-native-sortables/src/providers/shared/hooks/useItemPanGesture.ts
index 7aa4c06..1ecbd64 100644
--- a/node_modules/react-native-sortables/src/providers/shared/hooks/useItemPanGesture.ts
+++ b/node_modules/react-native-sortables/src/providers/shared/hooks/useItemPanGesture.ts
@@ -29,7 +29,13 @@ export default function useItemPanGesture(
       },
       onTouchesMove: (event, control) => {
         'worklet';
-        handleTouchesMove(event, control.fail);
+        handleTouchesMove(
+          event,
+          control.fail,
+          key,
+          activationAnimationProgress,
+          control.activate
+        );
       },
       onTouchesUp: (_event, control) => {
         'worklet';
diff --git a/node_modules/react-native-sortables/src/types/props/shared.ts b/node_modules/react-native-sortables/src/types/props/shared.ts
index cfbd78f..d01a982 100644
--- a/node_modules/react-native-sortables/src/types/props/shared.ts
+++ b/node_modules/react-native-sortables/src/types/props/shared.ts
@@ -73,6 +73,10 @@ export type ItemDragSettings = AnimatableProps<{
    * point before the drag is cancelled (only before drag starts)
    */
   dragActivationFailOffset: number;
+  /** Distance the touch must travel to activate the drag immediately, without
+   * waiting out `dragActivationDelay`. Infinity (the default) keeps the previous
+   * behaviour, where only the delay can activate. */
+  dragActivationDistance: number;
 }> & {
   /** Controls in which directions items can be dragged beyond container bounds */
   overDrag: OverDrag;
diff --git a/node_modules/react-native-sortables/src/types/providers/shared.ts b/node_modules/react-native-sortables/src/types/providers/shared.ts
index 4f99cc6..fd3ce5a 100644
--- a/node_modules/react-native-sortables/src/types/providers/shared.ts
+++ b/node_modules/react-native-sortables/src/types/providers/shared.ts
@@ -134,7 +134,13 @@ export type DragContextType = {
     activate: () => void,
     fail: () => void
   ) => void;
-  handleTouchesMove: (e: GestureTouchEvent, fail: () => void) => void;
+  handleTouchesMove: (
+    e: GestureTouchEvent,
+    fail: () => void,
+    key?: string,
+    activationAnimationProgress?: SharedValue<number>,
+    activate?: () => void
+  ) => void;
   handleDragEnd: (
     key: string,
     activationAnimationProgress: SharedValue<number>
